# Copy to .env and change passwords before compose up.
#   cp .env.example .env
DOCKER_STEPCA_INIT_PASSWORD=change-me-ca-password
PROVISIONER_PASSWORD=change-me-ca-password
PORTAL_USER=demo
PORTAL_PASSWORD=demo
