# Copy to credentials.txt (chmod 600) and fill real lab values after first init.
#   cp credentials.example.txt credentials.txt
# Portal login (simple local IdP)
PORTAL_URL=http://127.0.0.1:8080
PORTAL_USER=demo
PORTAL_PASSWORD=demo

# step-ca (must match .env)
STEP_CA_URL=https://127.0.0.1:9000
CA_NAME=SSH Lab CA
PROVISIONER_NAME=admin
DOCKER_STEPCA_INIT_PASSWORD=change-me-ca-password
PROVISIONER_PASSWORD=change-me-ca-password

# SSH target
SSH_HOST=127.0.0.1
SSH_PORT=2222
SSH_USER=demo
