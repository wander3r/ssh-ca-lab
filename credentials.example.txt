# Copy to credentials.txt (chmod 600). Used by scripts/ only.
PORTAL_URL=http://127.0.0.1:8088

# Local API issue (requires docker-compose.demo.yml / ALLOW_LOCAL_LOGIN=true)
PORTAL_USER=sre
PORTAL_PASSWORD=sre

STEP_CA_URL=https://127.0.0.1:9000
CA_NAME=SSH CA
PROVISIONER_NAME=admin
DOCKER_STEPCA_INIT_PASSWORD=change-me-ca-password
PROVISIONER_PASSWORD=change-me-ca-password

SSH_HOST=127.0.0.1
SSH_PORT=22
SSH_USER=sre
SRE_PASSWORD=change-me-sre-unix-password
