# Copy to inventory/hosts.yml (this file is the example; edit in place or
# maintain a private inventory outside git).
all:
  children:
    ssh_canary:
      hosts: {}
      # app-dev-1.example.com:
    ssh_fleet:
      hosts: {}
      # app-prod-01.example.com:
    ssh_hosts:
      children:
        ssh_canary:
        ssh_fleet:
    workstations:
      hosts: {}
